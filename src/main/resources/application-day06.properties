# Reuse the MySQL connection from application.properties and retain day06 tables.
spring.jpa.hibernate.ddl-auto=update

# practice3.sql contains Movie data, so it must not run for the day06 entity set.
spring.sql.init.mode=never
